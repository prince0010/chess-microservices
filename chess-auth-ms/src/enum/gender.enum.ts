export enum Gender {
  MALE = 'male',
  FEMALE = 'female',
  PRIVATE = 'private', // refers to the user who prefers not to say it
}

export const genderArray = [Gender.MALE, Gender.FEMALE, Gender.PRIVATE];
